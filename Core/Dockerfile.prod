# Core 서비스 Production Dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY app/ /app/
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt && pip install --no-cache-dir uv
CMD ["uv", "icorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
